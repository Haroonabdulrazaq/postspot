<% if flash[:notice]%>
    <div class="notification is-primary has-text-white global-notification mb-0">
      <p class="alert"><%= notice %></p>
    </div>
  <% end %>
  <% if flash[:alert]%>
    <div class="notification is-danger has-text-white global-notification mb-0">
      <p class="alert"><%= alert %></p>
    </div>
<% end %>